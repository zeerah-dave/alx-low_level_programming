#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char list[26] = "abcdefghijklmnopqrstuv";
	int i;
	for(i = 0; i < 26; i++);
	{
		putchar(i);
	}
	return (0);
}
