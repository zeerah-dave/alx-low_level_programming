#include "main.h"
#include <stdio.h>

/**
 *  _abs - Comuputes the absolute
 *  value of an integer.
 *
 *  Return: Always 0.
 */

 int _abs(int)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * -1)
	}
}
